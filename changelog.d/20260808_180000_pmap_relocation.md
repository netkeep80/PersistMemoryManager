---
bump: minor
---

### Fixed
- Make persistent and embedded `pmap` binding survive whole-arena relocation, replace allocating named construction with explicit `bind_domain()`, and preserve PMM-backed key/value inputs across relocating insertions while keeping the persistent `pmap` layout unchanged.
