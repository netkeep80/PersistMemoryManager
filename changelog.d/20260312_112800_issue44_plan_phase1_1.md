---
bump: minor
---

### Added
- Exception safety enforcement for `create_typed<T>()` and `destroy_typed<T>()` via `static_assert` (Plan Phase 1.1, Issue #44)
  - `create_typed<T>(args...)` now requires `T(args...)` to be `noexcept` constructible
  - `destroy_typed<T>(p)` now requires `~T()` to be `noexcept` destructible
  - Compile-time error with clear message if type requirements are not met
- New test file `tests/test_plan_phase1_1.cpp` with comprehensive tests for noexcept type requirements
- New documentation file `docs/phase1_safety.md` describing Phase 1 safety improvements

### Changed
- Updated `docs/plan.md` to mark Task 1.1 as completed
- Updated `include/pmm/persist_memory_manager.h` with `<type_traits>` include and static_assert checks
- Regenerated single-header files in `single_include/pmm/` with the new safety checks
