---
bump: major
---

### Changed
- Moved `pmap` root ownership from the local `_root_idx` field to the canonical `container/pmap` forest-domain binding.

### Removed
- Removed `pmap::_root_idx` as a public/local root model.
